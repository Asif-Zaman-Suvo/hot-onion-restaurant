import React from 'react';

const OrderComplete = () => {
    return (
        <div>
            
            <h1>This is order complete page</h1>
            
        </div>
    );
};

export default OrderComplete;